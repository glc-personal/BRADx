{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConfigFiles": {
    "Directory": "/app/configs",
    "Prefix": "ics",
    "Postfix": "config.xml",
    "Description": "All configuration files for the InstrumentControlService follow the format {prefix}.{controller}.config, e.g. ics.reader.config"
  }
}
